import { makeRegieGrid } from "./regie-common";

export const regiePilotageGrid = makeRegieGrid({
  serviceId: "regie-pilotage",
  label: "Régie — Pilotage & Chefferie de projet",
  low: 500,
  high: 850,
});
