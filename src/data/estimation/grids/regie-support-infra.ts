import { makeRegieGrid } from "./regie-common";

export const regieSupportInfraGrid = makeRegieGrid({
  serviceId: "regie-support-infra",
  label: "Régie — Support & Infrastructure",
  low: 350,
  high: 780,
});
