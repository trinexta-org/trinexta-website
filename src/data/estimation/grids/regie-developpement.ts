import { makeRegieGrid } from "./regie-common";

export const regieDeveloppementGrid = makeRegieGrid({
  serviceId: "regie-developpement",
  label: "Régie — Développement",
  low: 350,
  high: 950,
});
